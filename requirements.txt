# Note: tkinter comes built-in with Python, no need to install separately
# inflect removed - using custom number-to-words function to avoid PyInstaller issues
tkcalendar==1.6.1
babel==2.15.0
pyinstaller==6.9.0
